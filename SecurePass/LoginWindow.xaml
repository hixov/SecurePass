<Window x:Class="SecurePassWPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход" Height="250" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#FF2D2D30" WindowStyle="ToolWindow">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button x:Name="BrowseButton" Content="Обзор..." DockPanel.Dock="Right" Click="BrowseButton_Click"/>
            <TextBox x:Name="FilePathTextBox" IsReadOnly="True" Text="Выберите или создайте файл хранилища..."/>
        </DockPanel>

        <Label Grid.Row="1" Content="Мастер-пароль:" Foreground="White"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Margin="0,0,0,20"/>

        <Button x:Name="OkButton" Grid.Row="3" Content="OK" IsDefault="True" VerticalAlignment="Bottom" Height="30" Click="OkButton_Click"/>
    </Grid>
</Window>